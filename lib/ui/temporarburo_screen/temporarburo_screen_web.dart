import 'package:flutter/material.dart';

class TemporarburoScreenWeb extends StatelessWidget {
  const TemporarburoScreenWeb({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
