import 'package:flutter/material.dart';

class TemporarburoScreenTab extends StatelessWidget {
  const TemporarburoScreenTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
